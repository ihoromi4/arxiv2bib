[project]
name = "arxiv2bib"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "arxiv",
    "requests",
    "bibtexparser",
    "rich",
    "click",
]

[project.scripts]
arxiv2bib = "arxiv2bib.cli:main"
